- Make use of "sp.vstack" generic in `sparse_slice`
- Assert that L and R must be permutations in `sparse_slice` `HAR.__init__`
  method
- Provide a generic "permutation" diagram as a primitive, instead of just the
  twist(?)
